#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../tar.gz/bin/targz" "$@"
  ret=$?
else 
  node  "$basedir/../tar.gz/bin/targz" "$@"
  ret=$?
fi
exit $ret
